class Player(object):
    posX = 1
    posY = 1

    def SetPosition(self, x, y,):
        self.posX = x
        self.posY = y

    def GoToInput(self, input):
        #TODO: Parse input into new position and return new position if possible
        return False
